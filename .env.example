# Bu dosyayı kopyala → .env.local yap, değerleri doldur
# .env.local ASLA git'e commit edilmez (.gitignore'da olmalı)

NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
NEXT_PUBLIC_ADSENSE_SLOT=XXXXXXXXXX

DOWNLOAD_SECRET=              # node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
FILE_track1=                  # https://storage.example.com/dosya.mp3

ALLOWED_ORIGIN=https://tejreed.vercel.app
TOKEN_TTL_SECONDS=60
RATE_LIMIT_PER_MINUTE=5
